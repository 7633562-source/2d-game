# History — knee flipped

Knee `0…+120`, pelvis still **without** world control. Not acceptance.
Do not delete.

Runs `final_*`, `flip_*`. Numbers are worse than later `pelvis-world/`
and `stable-pd/`. Do not treat this as the goal. JSON and CSV sit
together — keep both.
